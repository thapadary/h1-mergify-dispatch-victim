pull_request_rules:
  - name: h1-unmerged-config-must-not-dispatch
    conditions:
      - -draft
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: h1-dispatch-oracle.yml
              ref: main
              inputs:
                marker: "{{ title }}"
